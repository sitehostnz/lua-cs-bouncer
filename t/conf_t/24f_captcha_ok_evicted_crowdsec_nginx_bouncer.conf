# A working captcha provider and a captcha decision, so captcha_ok is genuinely true
# at init. The test then deletes it from the shared dict to simulate LRU eviction,
# which is the only way that flag becomes nil in production.
APPSEC_URL=
ENABLED=true
API_URL=http://127.0.0.1:8081
API_KEY=LFrdL+aiecMTSxpGE9vLkx5sGMwdIpgVovpVMfXp3J0
CACHE_EXPIRATION=1
BOUNCING_ON_TYPE=all
FALLBACK_REMEDIATION=ban
REQUEST_TIMEOUT=3000
UPDATE_FREQUENCY=10
MODE=live
# /_evict is a test-only white-box delete against the decision cache
EXCLUDE_LOCATION=/v1/decisions,/_evict
BAN_TEMPLATE_PATH=./ban
REDIRECT_LOCATION=
RET_CODE=
CAPTCHA_PROVIDER=altcha
SECRET_KEY=
SITE_KEY=
CAPTCHA_TEMPLATE_PATH=templates/captcha.html
CAPTCHA_EXPIRATION=3600
ALTCHA_COST=100
